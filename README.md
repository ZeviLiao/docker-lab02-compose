
# docker-lab02-compose

docker-compose config file.  
docker-compose.yml

command to run
```
docker-compose up --build
```

background mode
```
#start up 
docker-compose up -d

#shut down
docker-compose down
```